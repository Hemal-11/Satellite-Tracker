# ============================================
# backend/.env.example
# Copy this file to backend/.env and fill in
# real values. NEVER commit backend/.env.
# ============================================

# Comma-separated list of allowed frontend origins.
# Set to * for public read-only APIs (default),
# or lock down to your deployed frontend URL.
ALLOWED_ORIGINS=http://localhost:5173
